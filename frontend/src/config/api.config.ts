export const API_BASE_URL = 'http://localhost:3001/api';

export const API_ENDPOINTS = {
  devices: `${API_BASE_URL}/devices`,
  health: `${API_BASE_URL}/health`,
  sleep: `${API_BASE_URL}/sleep`,
  userDevices: `${API_BASE_URL}/user-devices`,
};
